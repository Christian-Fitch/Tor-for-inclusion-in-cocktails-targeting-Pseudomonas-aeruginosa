This directory contains all the rawdata required to recreate the figures. The quarto document should be run as from the main directory in this system.

Note that the following rawdata directories contain compressed files that need to be extracted into their current directories (don't make any new subdirectories):
  figure1B
  figure1C
  
